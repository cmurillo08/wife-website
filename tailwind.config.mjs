// Placeholder palette — swap these for her real brand colors once she sends them (see docs/plans/phase-0-environment.md).
const config = {
  content: [
    './app/**/*.{js,jsx}',
    './components/**/*.{js,jsx}',
  ],
  theme: {
    extend: {
      colors: {
        primary: '#1F2937',
        secondary: '#F97316',
        'brand-bg': '#FAFAF7',
      },
    },
  },
  plugins: [],
}
export default config
